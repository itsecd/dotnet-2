<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:BotClient.ViewModels" x:Class="BotClient.Views.LoginWindow"
        mc:Ignorable="d"
        Title="LoginWindow" Height="156" Width="406">

    <Window.DataContext>
        <viewModels:LoginViewModel/>
    </Window.DataContext>

    <Grid Height="243" VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="592*"/>
            <ColumnDefinition Width="57*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="0*"/>
            <RowDefinition Height="0*"/>
        </Grid.RowDefinitions>
        <StackPanel VerticalAlignment="Top" Margin="0,28,0,0" Height="148" Grid.ColumnSpan="2">
            <Label Content="Enter your login:" HorizontalAlignment="Left" Height="33" Width="102"/>
            <TextBox Height="21" Width="390" Text="{Binding UserId}"/>
            <Button Command="{Binding OkCommand}" 
                    CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                    Content="OK" Width="391"/>
        </StackPanel>
    </Grid>
</Window>