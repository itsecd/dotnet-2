<Window x:Class="TaskClientWPF.Views.TasksView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:TaskClientWPF.ViewModels"
        mc:Ignorable="d"
        Title="Задачи" Height="450" Width="800">
    <Window.DataContext>
        <viewModels:TasksViewModel/>
    </Window.DataContext>
    <Grid>
        <DataGrid ItemsSource="{Binding Tasks}" SelectionMode="Single" SelectedItem="{Binding SelectedTask}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Задача" Binding="{Binding }"
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
